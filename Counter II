var createCounter = function(init) {
    let currentValue = init

    return {
        increment(){
            return currentValue+=1
        },

        decrement(){
            return currentValue-=1
        },

        reset(){
            return currentValue = init
        }
    }

};
