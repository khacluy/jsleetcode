/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    var mang= new Array();

    for(i=0;i<arr.length;i++){
        mang[i]=fn(arr[i],i);
    }

    return mang;
};
