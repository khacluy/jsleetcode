/**
 * @return {null|boolean|number|string|Array|Object}
 */
Array.prototype.last = function() {
   let d= this.length;
   if(d!=0)
   return this[d-1];
   else{
      return -1;
   }
};

/**
 * const arr = [1, 2, 3];
 * arr.last(); // 3
 */
