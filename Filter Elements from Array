/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
  
let filter = function(arr, fn) {
   let a = new Array();
  for(let i=0;i<arr.length;i++){
    if(fn(arr[i],i)){
        a.push(arr[i]);
    } 
  }
  return a;
};
